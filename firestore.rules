rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      allow read, write: if request.auth != null;
    }
    match /workers/{workerId} {
      allow read, write: if request.auth != null;
    }
    match /meal_lists/{listId} {
      allow read, write: if request.auth != null;
    }
  }
}
